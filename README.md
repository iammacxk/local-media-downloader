🚀 Local Media Downloader
เว็บแอปพลิเคชันสำหรับดาวน์โหลดวิดีโอ (MP4) และเพลง (MP3) คุณภาพสูงจาก YouTube, TikTok, X (Twitter), Facebook, Instagram (ไม่สามารถโหลด Stories ได้) และเว็บไซต์อื่นๆ อีกมากมาย รันบนเครื่องของคุณเอง (Localhost) ปลอดภัย และไม่มีโฆษณา

🛠️ สิ่งที่ต้องมีก่อนเริ่ม (Prerequisites)

ก่อนใช้งาน ต้องติดตั้งโปรแกรมพื้นฐาน 2 ตัวนี้:

1. Python
ต้องมี Python ติดตั้งในเครื่อง (แนะนำเวอร์ชัน 3.10 ขึ้นไป)
ดาวน์โหลด Python
สำคัญ: ตอนติดตั้ง อย่าลืมติ๊กถูกช่อง ✅ Add Python to PATH ด้วยนะครับ

2. FFmpeg (สำคัญมาก!)
ใช้สำหรับรวมภาพ+เสียง และแปลงไฟล์ ถ้าไม่มีตัวนี้จะโหลดได้ไม่สมบูรณ์
วิธีติดตั้งง่ายที่สุด (Windows 10/11):
เปิด Command Prompt (CMD) แล้วพิมพ์คำสั่ง:

winget install Gyan.FFmpeg

วิธีติดตั้งแบบ Manual (ถ้าวิธีบนไม่ได้ผล):
ดาวน์โหลด FFmpeg Essentials (ZIP)

แตกไฟล์ออกมา จะเจอโฟลเดอร์ bin ที่มีไฟล์ ffmpeg.exe
ย้ายไฟล์ทั้งหมดในโฟลเดอร์ bin ไปไว้ที่ C:\ffmpeg\bin (หรือแก้ Path ในไฟล์ app.py ให้ตรงกับที่คุณวาง)

📥 การติดตั้ง (Installation)
เปิดโฟลเดอร์โปรเจกต์
คลิกขวาในพื้นที่ว่าง > เลือก Open in Terminal (หรือเปิด CMD แล้ว cd เข้ามา)

ติดตั้ง Libraries ที่จำเป็น
พิมพ์คำสั่งนี้เพื่อลงตัวช่วยต่างๆ:

pip install flask yt-dlp


🚀 วิธีใช้งาน (Usage)

แบบง่าย (แนะนำ)
ดับเบิลคลิกที่ไฟล์ LocalDownloader.bat 
โปรแกรมจะรัน Server และเปิดหน้าเว็บให้เองอัตโนมัติ

แบบ Command Line
พิมพ์คำสั่งนี้ใน Terminal:
python app.py


แล้วเปิด Browser ไปที่ http://127.0.0.1:5000

🍪 วิธีแก้ปัญหาโหลดไม่ได้ (Cookies & Sensitive Content)

บางเว็บเช่น X (Twitter), Pornhub หรือ YouTube (Age-gated) จำเป็นต้องมีการล็อกอินเพื่อยืนยันอายุ หากโหลดไม่ได้ ให้ลองวิธีดังนี้:

วิธีที่ 1: ปิด Browser แล้วโหลด (The Close-Tab Trick)
เนื่องจาก Chrome/Edge จะล็อกไฟล์ข้อมูลไว้ตอนเปิดใช้งาน ทำให้โปรแกรมเข้าไปอ่านไม่ได้
ให้คุณเปิด Microsoft Edge
ล็อกอินเข้าเว็บนั้นๆ (เช่น X.com) และกด "ยอมรับ/I am 18+" ให้เรียบร้อย
ปิดโปรแกรม Edge ให้สนิท (ห้ามเปิดค้างไว้)

กลับมาใช้ Chrome เปิดหน้าเว็บ Downloader แล้วกดโหลด
(โปรแกรมจะแอบไปยืมข้อมูลจาก Edge ที่ปิดอยู่มาใช้)

วิธีที่ 2: ใช้ไฟล์ cookies.txt (แก้ถาวร - แนะนำ 👍)
วิธีนี้ทำให้คุณเปิด Browser ค้างไว้ได้ตลอดเวลา ไม่ต้องปิดๆ เปิดๆ
ติดตั้งส่วนเสริม "Get cookies.txt LOCALLY" ลงใน Chrome/Edge
เข้าเว็บที่มีปัญหา (เช่น X หรือ Pornhub)
กดที่ส่วนเสริม แล้วกดปุ่ม Export
นำไฟล์ที่ได้มาเปลี่ยนชื่อเป็น cookies.txt
นำไปวางไว้ในโฟลเดอร์เดียวกับ app.py

จบ! โปรแกรมจะใช้ไฟล์นี้ยืนยันตัวตนอัตโนมัติ

⚠️ คำเตือนสำหรับเว็บไซต์ผู้ใหญ่ (Adult Content)

บางเว็บโหลดได้ บางเว็บโหลดไม่ได้: ขึ้นอยู่กับระบบป้องกันของเว็บนั้นๆ
ต้องล็อกอิน: หากวิดีโอเป็นแบบ Premium หรือต้องเป็นเพื่อนเท่านั้น จะไม่สามารถดาวน์โหลดได้
เทคนิค: หากกดโหลดแล้ว Error ให้ลอง คัดลอกลิงก์ -> ปิดแท็บเว็บผู้ใหญ่นั้นทิ้งไป -> แล้วค่อยมากดดาวน์โหลด ในโปรแกรมนี้

❓ การแก้ไขปัญหาเบื้องต้น (Troubleshooting)

Error: ffprobe and ffmpeg not found
แปลว่าเครื่องหา FFmpeg ไม่เจอ ให้ลองติดตั้ง FFmpeg ใหม่อีกครั้ง หรือเช็ค path ในไฟล์ app.py

Error: I/O operation on closed file / Database locked
แปลว่า Browser (Chrome/Edge) เปิดค้างอยู่และแย่งไฟล์กัน
ทางแก้: ปิด Browser ที่ไม่ได้ใช้ หรือใช้วิธี cookies.txt

โหลดช้า / ค้าง
โปรแกรมมีระบบป้องกันการโดนแบน (Anti-Throttling) โดยการปลอมตัวเป็นมือถือและจำกัดการเชื่อมต่อ อาจจะช้าบ้างแต่ชัวร์ครับ

Developed with ❤️ using Python Flask & yt-dlp

By Tinnapat
